# Backend environment variables

# Mail credentials (use your SMTP provider)
MAIL_USER=
MAIL_PASS=

# Resend API Key (set this in Railway/hosting provider as RESEND_API_KEY)
RESEND_API_KEY=

# Server port (hosting platforms set PORT automatically)
PORT=5000
